function calculateShoppingCart(num1, ...num){
    return num;
}

// console.log(calculateShoppingCart(100, 200, 300))

const person = {
    name: "Joel",
    age: 19,
    gender: "Male"
}

function handleObject(useObject){
    return `Person name is ${useObject.name} and ${useObject.age} years old and he is ${useObject.gender}`;
}
// console.log(handleObject(person));

const myArr = ["Joel", "Manthan", "Mudit", "Rohit", "Abhishek (Bhaiya)"];

function handleArray(useArray){
    return useArray;
}

console.log(handleArray(myArr));
